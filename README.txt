vtags.el works pretty much like etags.el, but it recognizes
more types of tag files. See the comments at the top of the
file for current status.

Note: This latest version of vtags doesn't use regress.el
or the Test directory.